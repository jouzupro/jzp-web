import { HiraganaToRomajiPipe } from './hiragana-to-romaji.pipe';

describe('HiraganaToRomajiPipe', () => {
  it('create an instance', () => {
    const pipe = new HiraganaToRomajiPipe();
    expect(pipe).toBeTruthy();
  });
});
