import { RomajiToHiraganaPipe } from './romaji-to-hiragana.pipe';

describe('RomajiToHiraganaPipe', () => {
  it('create an instance', () => {
    const pipe = new RomajiToHiraganaPipe();
    expect(pipe).toBeTruthy();
  });
});
